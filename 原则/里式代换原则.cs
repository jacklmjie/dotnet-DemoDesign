namespace DesignMode.原则
{
    /// <summary>
    /// 子类型必须能够替换掉它们的父类型
    /// 只有当子类可以替换掉父类，软件单位的功能不受到影响时，父类才能真正被复用，
    /// 而子类也能在父类的基础上增加新的行为
    /// </summary>
    class 里式代换原则
    {
        //鸟是可以飞的，企鹅不会飞
        //如果鸟有飞的属性，企鹅就不能继承鸟
    }
}
